rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // App path: profileImages/{uid}/profile.<ext> (see AuthService)
    match /profileImages/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null
          && request.auth.uid == userId;
    }
  }
}
